# Используем официальный Python-образ (можно выбрать другую версию, например python:3.10)
FROM python:3.9-slim

# Устанавливаем рабочую директорию в контейнере
WORKDIR /app

# Копируем ваш серверный скрипт внутрь контейнера
COPY server.py .

# Открываем порт 8000 (по желанию, это скорее документация)
EXPOSE 8000

# Запускаем Python-сервер
CMD ["python3", "server.py"]
